version: '3.9'

services:

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: appdb

  redis:
    image: redis

  nginx:
    image: nginx
    ports:
      - "8086:80"